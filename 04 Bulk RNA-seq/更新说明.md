# V2 读图详解版 · 2026-09-15

本版在完整备份的复制件上修改。原目录 `转录组海岛教案` 和旧 ZIP 保留；另有完整备份 `转录组海岛教案_v1_备份_2026-09-15`。V2 新目录为 `转录组海岛教案_v2_读图详解版`，独立分享时不需要携带这些旧目录。

## 这次更新

1. **23 张真实图，149 个解释小节。** 每张均说明图形用途、实际数据范围、点线格子的单位、坐标与图例、真实数值和解释边界。摘要加粗并加背景，详解始终展开；放大窗口仍可对照同一份完整说明。
2. **补充 14 张原理插图。** 从重复与双端文件、FASTQ 质量字符、修剪与整条丢弃、decoy 竞争、转录本汇总、样本 PCA，到组内变异、集合读法、基因的两种视角、ORA 背景、GSEA 排队、模糊聚类、周期术语和注释映射。原有 5 张插图与 16 幅流程示意保留并补充说明。
3. **同步最新 V9 资料副本。** 20 个主章节、04.5 检查、5 份附录和 README，共 27 份说明文档；附 53 个实现脚本、10 个工具及小型入口表。每份复制文件有摘要记录，源码有完整阅读页。已完成的结果图和代表数据保留原值。
4. **两个中文入口。** `01_海岛探险教案.html` 用于探索学习；`02_完整教案_阅读与打印.html` 用于连续阅读和打印。所有内部返回、课程跳转与资料馆链接一并更新。
5. **浏览和默认构建均独立。** 将原来默认构建中的源项目读取移除，改为只从包内素材生成网页；以后更新资料使用单独的显式导入命令。

## 独立性与备份说明

原版的素材本来就已复制进包内。检查时发现根入口曾改名为 `教案.html`，但部分返回链接仍写旧入口名；V2 统一修复。原版构建程序会读取源分析项目，V2 已解除这一维护环节的依赖。

`qa/v1_backup_manifest.json` 记录开始修改前原版全部 177 个文件的 SHA-256 摘要，用于证明原版和备份未被覆盖。`素材来源.json` 核验当前包内的 148 条复制、提取或派生素材记录；`qa/docs_import.json` 单独记录本次资料快照。

`qa/` 中的静态、浏览器、插图、迁移构建及交付检查记录用于确认：本地路径未越出包、未自动请求外网、真实图完整加载、图解常显、原数据未重算。原始脚本的源路径、联网命令和未精选的完整结果说明是阅读内容，不是网页运行依赖。

示意图中的小数据为明确标记的教学示例；真实结果的颜色、阈值和数量按具体图与包内原表解释。特别区分了样本点与基因点、reads 与片段、ORA 命中数与 GSEA 核心基因数、Top30 与显著候选、模块中位数与周期均值连线。
