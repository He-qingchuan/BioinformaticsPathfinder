## 清理后的片段，怎样变成表达量

假设你找到很多书页碎片，想知道每本书原本出现了多少份。如果一段文字只在一本书里出现，很容易归类；如果几本书共享相同段落，这片碎纸就有多个可能来源。

RNA 读段也有类似问题：同一个基因可能产生多个相似转录本，不同基因也可能共享相似序列。**Salmon 的定量，是在这些可能来源之间进行模型估计。** 不能把同一片段完整地重复计给所有匹配对象。

{{diagram:quant}}

{{visual:assignment}}

## 参考、索引与定量分别是什么

**参考转录本**定义我们准备估计哪些 RNA 形式。**索引**把参考整理成便于快速查找的结构，像给书的内容建立搜索目录。**定量**则是拿某个文库的 clean reads 查询并估计丰度。

同一参考与索引可以用于多个文库，但每个文库要分别定量。把不同生物学重复合在一起定量，会丢掉后面估计组内变异所需要的独立观察。

## 为什么参考中还放入基因组 decoy

有的读段可能更像基因组中未列入参考转录本的其他区域。若只给它有限的转录本选项，就可能产生不合适的归属。

本流程建立 **decoy-aware 索引**，把基因组序列作为诱饵候选。它帮助识别更适合这些区域的读段，减少不合理地分配到已注释转录本。诱饵不是一个额外的“高表达基因”。

`-k 31` 是索引使用的 k-mer 长度。可以把 k-mer 理解为长度为 k 的连续序列小片段。这不是要求把所有 reads 裁剪成 31 nt。

{{illustration:decoy}}

## 模型为什么还需要偏倚校正

测到多少片段，不仅取决于 RNA 丰度，也受到片段长度、序列组成和实验过程影响。Salmon 结合片段与转录本的兼容性、有效长度及相关模型估计丰度，本案例还使用 GC 和序列偏倚校正。

文库类型参数 `-l A` 表示自动判断文库类型。双端文件仍必须正确配对，软件自动判断不能修复错误的样本身份或混配的 R1/R2。

## 看懂 quant.sf 的关键列

| 字段 | 含义 | 读图时要避免的误解 |
| --- | --- | --- |
| Name | 转录本 ID | 不是样本名 |
| Length | 转录本长度 | 不是测序 read 数 |
| EffectiveLength | 模型中的有效长度 | 不是随意裁剪后的长度 |
| TPM | 相对丰度 | 不是绝对 RNA 分子数 |
| NumReads | 估计分配的片段计数 | 可以有小数，不是把实际 reads 切成半条 |

一条模糊归属的片段可以按估计概率分配，因此 `NumReads` 出现小数并不奇怪。下一站再把转录本汇总到基因，得到跨样本矩阵。

## 本案例的一份真实定量记录

`ZT4_rep_1` 的 Salmon 记录处理 **41,025,421 个配对片段**，映射 **30,545,481 个**，映射率约 **74.46%**。处理片段数与上一站两个端合计的 **82,050,842 reads** 正好体现两种计数口径。

映射率不是差异表达显著性。参考覆盖、样本与参考差别以及片段本身的特征都可能影响它；不能把未映射部分直接全部归因于某一种原因，也不能把更高的映射率当作唯一优化目标。

<p class="resource-links"><a href="case/3Salmon/quanti/mapping_summary.tsv" download>下载 21 个文库的定量诊断表</a></p>

> 带走一句话：定量把序列观察转成丰度估计；参考范围、片段歧义和计数单位决定这些数应该怎样解释。
